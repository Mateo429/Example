#pragma once     //Se inclye este encabezado para que la decaracion no se propague a más archivos.
#include<vector>

double mean(std::vector<double> & data); //Declaramos la función que calculará el promedio de los vectores. 
